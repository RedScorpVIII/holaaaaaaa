class Cuenta:
    def __init__(self, saldo):
        self.__saldo = saldo   # atributo privado

    def depositar(self, monto):
        self.__saldo += monto

    def ver_saldo(self):
        return self.__saldo

c = Cuenta(100)
c.depositar(50)
print(c.ver_saldo())

